{
  "name": "oconnect-lookup-proxy",
  "version": "1.0.0",
  "description": "EAN -> {name,image} lookup proxy for O-CONNECT receiving app",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "engines": { "node": ">=18" },
  "dependencies": { "express": "^4.19.2" }
}
